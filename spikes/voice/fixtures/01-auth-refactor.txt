Refactor the authentication middleware in the DevinX TypeScript app. Validate every inbound payload with Zod and keep the existing OAuth callback behavior. Protected routes must perform their authorization check on the server, return a not found response when the caller cannot access a resource, and never reveal whether another tenant owns it. Keep tokens in Secure Store, not local storage, and remove any debug logging that could include an authorization header. Add focused tests for an expired token, a malformed callback, a cross-tenant identifier, and a successful refresh. Do not change the public route names or install another package. Open a PR with a concise security review and the exact commands used for validation.
