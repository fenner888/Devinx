Prepare the monorepo for the next TestFlight build without changing package versions. Run the mobile lint, TypeScript typecheck, unit tests, dependency audit, and secret scan from the repository root. Fix only regressions caused by the current branch. Confirm the EAS production profile increments the iOS build number and that the app archive uses the expected bundle identifier. In GitHub Actions, preserve the existing cache keys and make the failing job print a useful summary without exposing environment variables. The finished PR should include the build number, commit SHA, validation results, known moderate audit findings, and a rollback note. Do not publish to the App Store or merge the PR automatically.
