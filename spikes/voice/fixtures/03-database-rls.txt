Audit the PostgreSQL migration for the workspace membership table. Enable RLS and write policies so a signed-in user can read only organizations they belong to. The service role can perform the background synchronization, but ordinary clients cannot bypass the policy by guessing a UUID. Update the database types, add an index for organization ID and created at, and explain whether the query planner uses it. Test inserts, updates, deletes, and cross-tenant reads with two users in different organizations. If an unauthorized record is requested through the API, return not found rather than forbidden. Do not rename existing columns, reset production data, or place a service key in an Expo environment variable.
