Investigate why the Devin consumption view does not show the current organization limit. Compare the documented api.devin.ai version three organization endpoints with the enterprise billing endpoints, and confirm which call requires Manage Billing. Parse every response with Zod, follow cursor pagination with a hard page cap, and treat a missing permission as a graceful locked state rather than a crash. Show current-cycle ACU usage and remaining capacity in the app when the API exposes them. Keep the web page as a secondary billing-management link only for values the public API cannot return. Add endpoint and schema fixtures, including kebab-case repository names, a null product value, a rate limit, and an organization with no configured cap.
