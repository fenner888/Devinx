In the Expo Router session detail screen, fix the message composer so it stays above the keyboard and preserves a draft when the app backgrounds. Use the existing React Native theme tokens and TanStack Query cache. When I send a steering message, dismiss the keyboard, show the Devin companion walking directly above the composer, and stop the animation cleanly when the reply arrives. Do not restart the sprite animation on every typed character. Verify Reduce Motion, VoiceOver labels, dark and light themes, and an iPhone with a Dynamic Island. Keep the current route structure and do not touch the Devin API credentials. The acceptance criteria are no layout shift, no duplicate message, and no broken static image import.
